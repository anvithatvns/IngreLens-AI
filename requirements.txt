# IngreLens AI — Dependencies (all free tier)
# Install: pip install -r requirements.txt

# ── Core UI ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── HTTP / API ────────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0

# ── AI / ML — local, no API key needed ───────────────────────────────────────
sentence-transformers>=2.7.0
scikit-learn>=1.4.0

# ── Vector Database — free, local ────────────────────────────────────────────
chromadb>=0.5.0

# ── Data & Visualization ─────────────────────────────────────────────────────
pandas>=2.0.0
plotly>=5.18.0

# ── Image / OCR ───────────────────────────────────────────────────────────────
Pillow>=10.0.0
pytesseract>=0.3.10   # also requires: sudo apt-get install tesseract-ocr

# ── MCP Server — exposes the Coordinator Agent as MCP tools ──────────────────
mcp[cli]>=1.28.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-cov>=4.0.0
psutil>=5.9.0

# ── Optional: LLM providers (set API key in .env to activate) ────────────────
# openai>=1.0.0
# anthropic>=0.25.0
